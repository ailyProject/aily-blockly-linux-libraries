{
  "name": "aily-blockly-linux-libraries",
  "private": true,
  "description": "Python Blockly libraries for CyberCAM, Raspberry Pi, and WalnutPi.",
  "scripts": {
    "test": "npm test --prefix cybercam && npm test --prefix python-core && npm test --prefix linux-python"
  }
}
